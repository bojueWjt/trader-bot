# Database Migrations

Bridge migrations are staged here. The current test implementation uses in-memory repositories, and durable Signal Store migrations remain a bridge follow-up before production data retention.
