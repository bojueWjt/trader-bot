server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: history.pushState routes must fall back to index.html on refresh
    location / {
        try_files $uri $uri/ /index.html;
    }
}
