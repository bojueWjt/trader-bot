# 已知缺口表（INTEGRATION_REPORT 用 · 草稿）

> **状态：草稿。** P1 未收口（R-10 五审 fail），本表先行累积，随 OR-06 定稿。
> 依据：契约 §5.20 B18（残余须带声明）、§9.10.19 A39（方法边界属契约）、§19 A33/§18 A32（不适用须记理由/债主/触发事件）、§25.4（审查 open 项转写为失败测试属进度）。
> **本表只记「已知且已判定」的缺口，不记未决争议。** 每条须可追溯到契约条款或审查编号。

## 1. 验证方法的边界（属方法，不属某次运行）

| 缺口 | 性质 | 依据 |
|---|---|---|
| 机器判读证明不了**计算发生过** | 哈希证明"由哪版代码生成"，自洽性证明"数字不矛盾"，**两者都不证明数字是跑出来的**。一份改写全部字段又用当前代码重排版的伪造报告可通过每一道门 | §9.10.19 A39 |
| 对抗手段为**独立重采样比对**，且**未在 P1 执行** | 报告已公开 world/pipeline 配置与每机制 seed 段（`seed0×100000+i`），P2 前置；比对口径落**结论层**（阳性计数与 CP 区间同判定侧），非浮点逐位 | A39 §3 + changeLog #69 |
| 三道机制（禁令/登记/差分）**互补但非穷尽** | 实测：差分单独 1/4、三道合计 4/4；**边界类须逐类以判别性突变证明覆盖**，未给出该突变的类视为未覆盖 | §18 A32、§22 A36 |

## 2. 范围边界（属本次交付）

| 缺口 | 现状 | 债主 / 触发事件 |
|---|---|---|
| **G3 未接入 `force_close_net_R`** | 跨窗口调用登记与使用点计数**均为空集**（裁定结果，不得预登记不存在的调用方）；差分列"无消费方，不适用" | 债主 G3；触发事件：G3 接入时须以哨兵证明 θ 随返回值改变 |
| **真实归档验证范围仅 BTCUSDT 2024-01** | bar 类 44640×2 行离网 0；funding 93 行离网 15（16.1%） | P2 扩品种/扩月 |
| **M-11 gated 未启动** | 按用户指令不做，market 通道停在 0.91——**这是有意的边界，不是未完成任务** | 用户闸门 |
| **D-08 / D-11 gated 未启动** | 同上，受用户闸门限制 | 用户闸门 |

## 3. 统计与口径（须随 `G-STAT-CLAIM` 确认）

| 项 | 裁定 | 未决部分 |
|---|---|---|
| **部分出场 estimand** | 主口径 B（观察终点强制平仓），A 作**强制敏感性**；任何 θ 声明须写明所用 estimand | P2 随闸门确认最终口径 |
| **`research_horizon_s` = 5 天** | 占位已解除；真实括号下 3d 删失 15.3% / 5d 6.0% / 7d 3.4% | 基于 5d 的 θ 声明须满足每折 Q×5 天日历（Q=20 → 100 天）；**2024-01 单月仅 5 块，任何合理下限都过不了——这是数据量结论不是取值结论** |
| **功效未达标** | δ=0.2R 下约 40%，低于 DoD 的 80%；按用户裁定的**出口 A**（限制声明式闭合）处理 | 限制声明须随任何 θ 声明一并出具 |
| **最小有效样本下限** | 口径 = `maxt.calendar_blocks` 的 `n_nonempty`，须与 `block_len` **成对声明**且 `block_len ≥ horizon + embargo`，同时钉 `cross` 与 `max_span` | 具体数值待 G2+G3 联合提案 |

## 4. 各窗口 partial-P2 残余

| 窗口 | 残余 | 说明 |
|---|---|---|
| G2 (market) | 10 项 partial-P2（S02/S04/S06/S07/S10/S11/S12/S13/S34/S36） | 十四审终裁 **pass**；`P2_UNSUPPORTED` 四键如实声明 |
| G1 (data) | 七项经用户裁定降 P2 | D-10 经 G0 分类闭合裁定 done（`ruling-G0-D10-classification-closure.md`） |
| G3 (research) | **R-10 五审 fail，尚未收口** | S08 闭合、S17/S10 部分闭合；核心未闭合项：**结构失败未强制纳入 `n_failed`，能使条件 FPR 最坏界假绿** |

## 5. 定稿前须补

- [ ] R-10 收口后，补 G3 的最终残余清单与终裁引用
- [ ] `readyForStitch` 判定与依据
- [ ] 三窗口 verify 的最终实跑输出（G0 亲跑，附测量时刻与 A40 频率判据结果）
