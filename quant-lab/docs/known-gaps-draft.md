# 已知缺口表（INTEGRATION_REPORT 用 · 草稿）

> **状态：草稿。** P1 未收口（R-10 **六审** fail：R6-G/R6-H/R6-L 三条必修），本表先行累积，随 OR-06 定稿。
> 依据：契约 §5.20 B18（残余须带声明）、§9.10.19 A39（方法边界属契约）、§19 A33/§18 A32（不适用须记理由/债主/触发事件）、§25.4（审查 open 项转写为失败测试属进度）。
> **本表只记「已知且已判定」的缺口，不记未决争议。** 每条须可追溯到契约条款或审查编号。

## 1. 验证方法的边界（属方法，不属某次运行）

| 缺口 | 性质 | 依据 |
|---|---|---|
| 机器判读证明不了**计算发生过** | 哈希证明"由哪版代码生成"，自洽性证明"数字不矛盾"，**两者都不证明数字是跑出来的**。一份**全字段协调、且数学上可能**的伪造报告可通过每一道门 | §9.10.19 A39 |
| A39 的边界覆盖**对抗**，不覆盖**事故** | 正常导入缓存、陈旧 `__pycache__`、fork 继承父进程模块对象所致的**混版**无需恶意即会发生，且同一机制即可挡住；把"对抗下无法证明"读作"事故也不必预防"是拿边界当免责 | §9.10.20（六审 R6-H 起） |
| **数学上不可能**的伪造值不在 A39 边界内 | 派生统计量继承被汇总对象的可行域：相关系数恒在 [-1,1] ⇒ 样本 sd 有硬上界 `s²≤n/(n−1)(1−m²)`。超界值不必重跑 MC 即可否证，属**报告自身的可实现性约束** | §25.9 A41（六审 R6-G） |
| 磁盘摘要**不等于**执行摘要 | `research_code_digest` 答"文件现在长什么样"，`executing_code_digest` 答"这个进程执行哪一版"；A31 运行期漂移检查由前者承担，回执的执行身份由后者承担，**前者不得被描述为证明了后者** | §9.10.20 第 2 条 |
| 对抗手段为**独立重采样比对**，且**未在 P1 执行** | 报告已公开 world/pipeline 配置与每机制 seed 段（`seed0×100000+i`），P2 前置；比对口径落**结论层**（阳性计数与 CP 区间同判定侧），非浮点逐位 | A39 §3 + changeLog #69 |
| 三道机制（禁令/登记/差分）**互补但非穷尽** | 实测：差分单独 1/4、三道合计 4/4；**边界类须逐类以判别性突变证明覆盖**，未给出该突变的类视为未覆盖 | §18 A32、§22 A36 |

## 2. 范围边界（属本次交付）

| 缺口 | 现状 | 债主 / 触发事件 |
|---|---|---|
| **G3 未接入 `force_close_net_R`** | 跨窗口调用登记与使用点计数**均为空集**（裁定结果，不得预登记不存在的调用方）；差分列"无消费方，不适用" | 债主 G3；触发事件：G3 接入时须以哨兵证明 θ 随返回值改变 |
| **OR-04 e2e 是非退化门，不是回归门** | 实际 vs 下限：成交 **78/80** vs 40（余量 1.95×）、走完结局 **65/80** vs 20（余量 3.25×）。走完结局掉到 21 仍全绿（损失 68% 不报警） | 债主 **G0**；触发事件：P1 收口后把实际值写为基线并评估带容差的基线带（§25.16 A46）。**凡引用「integration 8 passed」处须同时给出本行数值** |
| **真实归档验证范围仅 BTCUSDT 2024-01** | bar 类 44640×2 行离网 0；funding 93 行离网 15（16.1%） | P2 扩品种/扩月 |
| **M-11 gated 未启动** | 按用户指令不做，market 通道停在 0.91——**这是有意的边界，不是未完成任务** | 用户闸门 |
| **D-08 / D-11 gated 未启动** | 同上，受用户闸门限制 | 用户闸门 |
| **损耗表与隔离区读侧无恒等校验** | `api.py:91 loss_table` 与 `api.py:101 quarantine` 均为直读 parquet，**读侧无平衡/恒等校验**；恒等式由 G1 构建期测试（S12，已闭合）保证。手改或半写的损耗 parquet 会被原样返回 | 债主 **G1**；触发事件：读侧接入恒等校验或发布期封存。按 §9.10.20 属**事故面**（半写/陈旧无需恶意即发生），非对抗面 |
| **路径逃逸覆盖为枚举式** | 7 类逃逸向量全 REJECT 且拒绝在取 hash 之前，但**是枚举覆盖，非「任何路径」的类级证明** | 按 A44 如实降格；结构有界且拒绝点靠前，不升必修 |

## 3. 统计与口径（须随 `G-STAT-CLAIM` 确认）

| 项 | 裁定 | 未决部分 |
|---|---|---|
| **部分出场 estimand** | 主口径 B（观察终点强制平仓），A 作**强制敏感性**；任何 θ 声明须写明所用 estimand | P2 随闸门确认最终口径 |
| **`research_horizon_s` = 5 天** | 占位已解除；真实括号下 3d 删失 15.3% / 5d 6.0% / 7d 3.4% | 基于 5d 的 θ 声明须满足每折 Q×5 天日历（Q=20 → 100 天）；**2024-01 单月仅 5 块，任何合理下限都过不了——这是数据量结论不是取值结论** |
| **功效未达标** | δ=0.2R 下约 40%，低于 DoD 的 80%；按用户裁定的**出口 A**（限制声明式闭合）处理 | 限制声明须随任何 θ 声明一并出具 |
| **最小有效样本下限** | 口径 = `maxt.calendar_blocks` 的 `n_nonempty`，须与 `block_len` **成对声明**且 `block_len ≥ horizon + embargo`，同时钉 `cross` 与 `max_span` | 具体数值待 G2+G3 联合提案 |

## 4. 各窗口 partial-P2 残余

| 窗口 | 残余 | 说明 |
|---|---|---|
| G2 (market) | 10 项 partial-P2（S02/S04/S06/S07/S10/S11/S12/S13/S34/S36） | 十四审终裁 **pass**；`P2_UNSUPPORTED` 四键如实声明 |
| G1 (data) | **7 项：S08、S13、S14、V01、W01、W02、W03** | D-10 经 G0 分类闭合裁定 done（`ruling-G0-D10-classification-closure.md`）。**该裁定非审查方 pass**：`review-G1-P1.md` 及 r3/r4/r5 各轮终裁均 fail，D-10 的 verify 经**析取第二支**（G0 裁决书）通过；用户在 G1 会话经 `AskUserQuestion` 预授权该路径。裁定 §6 已更正两处：open 口径 3→**7**；S08/V01/W02 属**可复算性**必闭合类而原 §2 该格未覆盖录制解码路径，**已补判别性实跑**（`parse_float=Decimal` 去掉即静默改量 `-3E-12`），该格现已证明闭合 |
| G3 (research) | **R-10 六审 fail，尚未收口** | S01–S16/S18/S19 闭合；R5-W/S10（真实弱相关破坏）闭合。**三条必修**：R6-G 相关 sd 缺可实现性上界（单对 mean=0/sd=3 被接受，带宽 0.569 足以吞掉完整相关丢失）；R6-H 回执绑磁盘视图未绑已加载执行修订（两个真实 worker 执行缓存修订、首尾摘要全等仍发布）；R6-L 空 L 分布跳过记账门（`chosen_block_len={}` 使整个计数门旁路）。**R5-C 失败收费为「部分闭合」**：CP 上界拒收成立（最低正确收费 50 的 searched CP 0.08020158944615748 被 FPR 门拒），R6-L 是**绕过**而非推翻 |

## 5. 定稿前须补

- [ ] R-10 收口后，补 G3 的最终残余清单与终裁引用（**路径已裁定：走七审，不发 G0 裁决书——见 §25.12 A43**）
- [ ] `readyForStitch` 判定与依据
- [ ] 三窗口 verify 的最终实跑输出（G0 亲跑，附测量时刻与 A40 频率判据结果）
