# Optional drop-in for trader-v3-hermes-feeder.service.
# Install/restart only after the account-specific cutover review.
# Deploy ingress POST /telegram/raw/signal first; old ingress returns 404.
# No probe or legacy fallback. Removing opt-in preserves the durable freeze.
[Service]
EnvironmentFile=-/srv/trader-v3/secrets/signal-cutover.env
