# 工作入场限价与资金占用

RESUME、换品种开仓不得以撤销合法工作入场限价来满足门禁。机器人订单身份为 `^B[0-9a-f]{32}[0-9]{2}$`；用户手动订单（包括 `aos_*`）仍禁止自动撤销。门禁与投影不一致应修复证据处理，不能通过修改交易所订单簿绕开。

- RESUME 的工作限价豁免以场上 LIMIT/GTC/regular/明确非 reduce-only 的正价量形状，加 CID 对应账户、品种一致的 approved open/add 意图为准。投影缺列或价量不一致不能否定该证据。意图 TTL 到期不撤销已经在场的 GTC；HALT/RESUME 不要求清空合法挂单。
- 开仓资金占用先取既有交易所快照中的工作单，用剩余数量乘价格；同 CID 的旧投影只作补充，不能重复计算。同一意图的三条区间腿分别计入工作名义。
- SL/TP、reduce-only 和机器人 CID 序号 ≥10 的保护单不计开仓占用。filled/canceled/expired 腿不再预留，但仍证明该腿曾发送，不能将其重新算作“尚未发送”。
- 计划仅补有明确 seq 和名义的未发送腿。旧 `entry.type=zone` 不是一腿计划，不能把场上的 02/03 判成全账户未知。只剩保护单的意图占用为零。
- 未知工作腿或不完整多腿计划按该意图 `max_notional` 保守预留，与已知工作名义去重；后续按资金是否足够决定接受或 400 拒绝。无合法风险预算等真正损坏数据仍不得凭空放行。
- 单一 operator 入口在批准符合条件的区间阶梯时保存 `type=zone_ladder`、三档 seq、价量及名义；下发使用已批准三档，不因后来价格变化重新生成。

占用读取和 RESUME 豁免均不写交易所、不改订单投影、不创建撤单命令。新开仓的授权、品种风控、账户状态和资金上限继续生效；别的品种有挂单本身不是拒绝理由。

## 2026-09-15 验证记录

- 控制面全量：701 passed，1 subtest。
- 最后补充快照采集时间对齐、原始/裁剪两种交易所形状后，占用、资金预留和 recorder 专项：73 passed，1 subtest。
- 节点阶梯执行回归：9 passed（按 zone_ladder 筛选）。
- 真实临时 PostgreSQL 验证：三条已部分成交的场上限价覆盖薄壳投影，只计剩余名义；未知旧单按预算预留，资金不足返回 400、资金足够可通过；保护单和手动单不参与入场占用。占用计算前后快照内订单保持一致，没有产生撤单命令。
- RESUME 临时库测试覆盖投影缺价量、价量漂移、旧终态与当前在场证据冲突；同时验证批准保存三档，行情变化后下发三档不变。
- 此记录为本地代码与隔离测试验收，尚未部署，也没有执行生产 RESUME 或撤单。

## 2026-09-16 旧保护与品种冻结收敛

- 控制面开仓仓位检查只采用新鲜、有效的交易所镜像/心跳。历史 `positions_projection` 不再否决已确认空仓，也不为放行伪造平仓事件或改写历史账。镜像与心跳实际矛盾、证据过期或损坏仍拒绝。
- 旧单腿保护 stash 的 watchdog/sync 复用执行器的交易所快照；场上空仓或证据未知时，不再按旧缓存反复补幽灵保护。更新的本意图入场成交回执可以证明旧空仓快照已过时，正常新成交不等待下一轮 watchdog。场上有仓时，保护数量采用场上数量，缓存仅补充本地仓位标识。
- 单腿 stash 退役要求：新鲜空仓，所有已知入场及保护订单有终态证明，终态不晚于持仓快照，没有在场工作单、待确认回执、待撤单或 TP 市价后备任务。退役仅删除本地保护元数据，通过已有持久化路径保存；不发送撤单。双入场的归属账与迟到成交 tombstone 保留。
- `protection order repair failed twice` 仅在该品种所有相关保护已确认恢复（或旧 stash 安全退役）后解除。提交了修复请求不等于恢复；其他冻结原因与待确认回执继续阻止加风险。
- `v3_trade.py` 的 argparse 帮助字符串使用 `100%%`，将生产热修落回源码。

边界：`NEW`、找不到订单、`-2013` 或 `CancelConfirmationUnknownError` 都不是取消终态。仍缺证据的旧 stash 不自动清除；节点本地缓存与交易所的开仓规划冲突检查仍保留，需要真实对账收敛。相同 client_ref 的拒单重放保持幂等，不自动换 ref 重下。此改动不证明 B 账户现场已恢复，也不包括历史漏单补下、生产部署或 RESUME。

验证：控制面全套 523 项通过；随后新增的历史投影 HTTP 集成回归连同相关测试通过。执行域与交易脚本回归 465 项通过，覆盖终态退役、未知/在场/过期证据保留、双入场接替旧 owner、迟到成交、取消未知与原有幂等边界；Python 3.14.1 十个交易子命令 `--help` 全部通过。
